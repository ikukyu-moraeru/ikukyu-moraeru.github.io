import './steps.css'

export function Step5Result() {
  return (
    <div className="st-empty">
      <span className="st-empty__emoji" aria-hidden>
        ✨
      </span>
      第五章は、出産日ごとの判定結果を一覧する章です。
      <br />
      （次のコミットで scanBirthDates を接続して実装予定）
    </div>
  )
}
