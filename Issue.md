# Rule.md と現実装の不一致リスト

> 起票日: 2026-04-27
> 対象コミット: `bf4c0ad`
> 突合対象: `Rule.md` ⇔ `src/domain/`

各項目に「Rule.md の根拠 / 実装の現状 / 乖離 / 修正方針」を記す。優先度の高い順から着手する想定。

---

## 重大度: 高（判定結果が変わりうる）

### #1 80 時間ルールの適用判定の起点ずれ
- **Rule.md 根拠**: §7（制度改正タイムライン）／厚労省 LL020615 保 01「2020-08-01 以降に **離職／育休開始** の月から適用」
- **実装の現状**: `src/domain/eligibility.ts:198` `canApplyHoursRule(monthEnd) → monthEnd >= "2020-08-01"`。完全月の **終端日** で判定し、終端が 2020-08-01 より前の月は 80 時間ルールを適用しない。
- **乖離**: 育休開始日が 2020-08-01 以降なら判定対象期間（過去 2 年）すべての完全月にルールが適用されるはず。実装は完全月単位で当落を分けており、2020-07 以前で終わる完全月が誤って対象外になる。
- **テストへの影響**: `src/domain/__tests__/eligibility.test.ts` の「80 時間ルールは 2020-08-01 以降の月にしか適用されない」が、誤った挙動を「正」として固定している。テストごと書き換えが必要。
- **修正方針**: `judgeEligibility` の入口で `childCareStartDate >= "2020-08-01"` を一度だけ判定し、各月の判定にフラグとして渡す。`canApplyHoursRule(monthEnd)` を廃止。

### #2 基本手当受給資格決定の検出範囲が狭い
- **Rule.md 根拠**: §4-1 (イ)「最後に被保険者となった日前に、受給資格決定したことがある場合、当該受給資格に係る **離職の日以前** の被保険者であった期間」を全除外。
- **実装の現状**: `src/domain/carryOver.ts:91` `hasBasicAllowanceClaimedBetween` で **直前ギャップに重なる** `NonInsuredGap` の `basicAllowanceClaimed=true` だけを見て前職リセット判定。
- **乖離**: 過去の任意の時点での受給資格決定（例: 5 年前に離職 → 受給資格決定 → 復帰 → 5 年在職）を捕捉できない。判定対象期間外の決定は素通り。
- **修正方針**: 入力モデルに `basicAllowanceClaimedAt: DateISO` のような独立フィールドを設け、その日付以前のセグメントを `mergeInsuredSegments` 入口で全除外する。
- **入力モデル変更**: `UserInput.basicAllowanceClaims: DateISO[]` を追加（複数回ありうるため）。`NonInsuredGap.basicAllowanceClaimed` は冗長になる可能性 → 整理が必要。

### #3 Rule §4-1 (ロ) が完全に未実装
- **Rule.md 根拠**: §4-1 (ロ)「法第 9 条の規定による被保険者となったことの **確認があった日の 2 年前の日前** の被保険者であった期間」を除外。
- **実装の現状**: 該当ロジックなし、入力モデルにフィールドもなし。
- **影響**: 事後的に被保険者となっていたことが確認された（遡及加入）ケースで、確認日の 2 年前を超える過去は本来カウントできない。レアケースだが抜け漏れ。
- **修正方針**: `InsuredEmploymentSegment` に `confirmedAt?: DateISO` を任意追加し、`confirmedAt - 2 年` より前の期間を内部で切り詰める。レア度を考えると後回しでよい。

---

## 重大度: 中

### #4 「2 年」「1 年」が固定日数で扱われている
- **Rule.md 根拠**: §3-4「最長 **2 年間**（合計 4 年間）」／§4-2「離職日から **1 年以内**」
- **実装の現状**:
  - `relaxation.ts:24` `MAX_RELAXATION_DAYS = 730` 固定
  - `carryOver.ts:80` `gapDays > 365` 判定
- **乖離**: うるう年を跨ぐと「2 年」「1 年」は 731 / 366 日になりうるが、730 / 365 でクランプしている。最大 1 日のずれ。
- **修正方針**: 緩和は「`addYears(baseWindowStart, -2)` で日付を引いて差分日数を出す」、通算は「離職日 + 1 年（カレンダー）以内に再就職」を `addYears` で比較する形に直す。

### #5 端数月に 80 時間ルールが適用されない
- **Rule.md 根拠**: §3-3「期間の日数が 15 日以上 かつ 賃金支払基礎日数 11 日以上」のみ。80 時間ルールは明記なし。
- **実装の現状**: `eligibility.ts:judgeFragment` も 80 時間チェックなし。
- **乖離**: Rule 文面通りではあるが、§3-1 で 80 時間ルールが導入されている以上、端数月にも整合的に適用するのが自然という解釈もある（厚労省 LL020615 保 01 の射程次第）。
- **修正方針**: 厚労省パンフレット最新版を再確認。端数月への適用が明記されていれば追加、明記なしなら現状維持＋コードコメントで意図を明示。

### #6 緩和事由 (ニ)(ホ) の専用選択肢が UI にない
- **Rule.md 根拠**: §3-4 (ニ) 事業主の命による外国における勤務 / (ホ) 雇用継続交流採用
- **実装の現状**: `types.ts` `LeaveType = "産休" | "育休" | "病気休職" | "介護休業" | "組合専従" | "配偶者海外同行" | "事業所休業" | "その他"`
- **乖離**: (ニ) は「その他」で扱えるが、明示の選択肢がないため正しく分類入力されない可能性。
- **修正方針**: `LeaveType` に `"海外勤務"` `"雇用継続交流"` を追加し、Step2 の選択肢にも対応する emoji/label を加える。

---

## 重大度: 低（運用で吸収可）

### #7 完全月の被保険者判定が「全期間内包」で厳しめ
- **Rule.md 根拠**: §5-2 (6)「雇用保険被保険者であり」とのみ。期間粒度は曖昧。
- **実装の現状**: `eligibility.ts:isFullyInsured` 完全月 `[start, end]` がセグメントに **完全に内包** される必要。
- **乖離**: 完全月の 1 日でも未加入があれば「雇用保険未加入」とみなされカウント 0。実務上は安全側だが Rule から一意には決まらない。
- **修正方針**: 現状維持で OK。代わりに UI 側で「未加入」表示の根拠（どの日が未加入か）を明示するとユーザーの納得感が上がる。

### #8 「賃金支払基礎日数」と日別入力モデルのギャップ
- **Rule.md 根拠**: §0「賃金支払の基礎となった日数（有給日・休業手当対象日含む）」
- **実装の現状**: `DailyAttendance` で `work / paid_leave / paid_special / absent` を日付入力したものだけカウント。
- **乖離**: 月給制で「土日祝も賃金支払基礎日数に含まれる」ケースを表現できない。土日入力なしだと 0 寄与。
- **修正方針**: Step4 に「月給制（土日も基礎日数に含む）／日給月給制（出勤日のみ）」のトグルを追加し、月給制ではカレンダー全日を `work` 換算する補助ロジックを入れる。

### #9 産前休業の起点が「実出産日基準」になっている
- **Rule.md 根拠**: 出産手当金（§6-1）/ 労基法 65 条「**出産予定日** 以前 42 日（多胎 98 日）」
- **実装の現状**: `eligibility.ts:50` `leaveStartDate = subDays(birthDateD, prenatalDays)` で `birthDate`（実出産日相当のスキャン日）を基準。
- **乖離**: スキャン中の各日を「予定日 = 実出産日」とみなしているため産前休業開始日も日付ごとに動く。本来は予定日固定 + 実出産日変動。
- **影響**: 判定基準日 `childCareStartDate` の正しさが保たれていれば最終判定は変わらない。表示上の `leaveStartDate` が誤解を招く可能性のみ。
- **修正方針**: `judgeEligibility` の引数に `expectedBirthDate` を追加し、`leaveStartDate` は予定日基準で算出。`childCareStartDate` は実出産日基準のまま。

---

## 修正の着手順（推奨）

| 順 | 項目 | 理由 |
| -- | ---- | ---- |
| 1 | #1 80 時間ルール | テスト改修も伴うが、関数 1 つ書き換えればよく波及が小さい |
| 2 | #6 緩和事由の選択肢追加 | LeaveType に 2 値追加 + UI 微修正のみ |
| 3 | #4 「2 年」「1 年」の年計算正確化 | 1 日ずれの修正、影響限定 |
| 4 | #2 受給資格決定の検出範囲 | 入力モデル拡張が必要。設計議論が要る |
| 5 | #9 産前起点の予定日化 | 表示の正確化、判定結果は変わらない |
| 6 | #3 Rule §4-1 (ロ) 確認日 2 年前以前除外 | レアケース、最後でよい |
| 7 | #5 端数月の 80 時間ルール | 仕様判断（要源資料再確認） |
| 8 | #8 月給制トグル | UX 拡張寄り、判定本体には影響しない |
| 9 | #7 完全月内包の厳しさ | 文書化で済む可能性 |
